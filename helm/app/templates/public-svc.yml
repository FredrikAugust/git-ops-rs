apiVersion: v1
kind: Service
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: public-svc
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: "{{ .Chart.Name }}"
